# --relative=1 with --min_weight in {10,30,50,70}. relative makes the wtmul
# term scale (source_weight - min_weight), so min_weight here does double duty:
# it is both the source-selection weight floor AND the subtrahend in the
# transfer. Larger min_weight shrinks the relative transfer (and raises the
# floor). mw10 = default floor.
# name	args
rel1_mw10	--relative=1 --min_weight=10
rel1_mw30	--relative=1 --min_weight=30
rel1_mw50	--relative=1 --min_weight=50
rel1_mw70	--relative=1 --min_weight=70
