# Sweep over per-try flip cutoff. cutoff = flips per try before inner restart
# (assignment redraw + optional strategy randomization via --fixed). Smaller
# cutoff = more restarts = more diversification; larger cutoff = longer
# continuous search per try.
#
# Current solver default cutoff=300000. This sweep probes a wide log-scale
# range {100K, 1M, 10M, 100M} to see where the cutoff curve peaks after the
# recent perf/algorithm changes. Name encodes the cutoff.
# name	args
co100k	--cutoff=100000
co1m	--cutoff=1000000
co10m	--cutoff=10000000
co100m	--cutoff=100000000
