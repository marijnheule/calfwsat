# keep x reset_weights 2x2 sweep.
#
# keep (options.h default 0): at an inner restart, leave the current
#   assignment in place instead of re-picking (best/polarity/random).
# reset_weights (options.h default 1): on inner restart, snap all clause/card
#   weights back to their initial values instead of carrying the evolved
#   DDFW weights across the restart.
#
# The two are orthogonal. Each row pins both values explicitly, so the
# solver's own defaults do not affect the sweep. keep0_rst0 is the stock
# yal-lin baseline; keep0_rst1 is the current solver default.
# name	args
keep0_rst0	--keep=0 --reset_weights=0
keep1_rst0	--keep=1 --reset_weights=0
keep0_rst1	--keep=0 --reset_weights=1
keep1_rst1	--keep=1 --reset_weights=1
